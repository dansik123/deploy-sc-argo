apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-master-config
  namespace: "{{ .Values.appName }}-ns"
data:
  postgresql.conf: |
    wal_level = replica
    max_wal_senders = 3
    wal_keep_size = 64MB